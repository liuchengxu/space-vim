if LayerLoaded('lightline')

    " lightline.vim {
    if IsDir('lightline.vim')
        let g:lightline = {
                    \ 'colorscheme': 'wombat',
                    \ }
    endif

    " }
endif

